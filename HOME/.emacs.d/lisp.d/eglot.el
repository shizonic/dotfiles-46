;;; -*- lexical-binding: t; -*-

;; requires language server be installed https://github.com/joaotavora/eglot#connecting-to-a-server

(use-package eglot
  :init
  ;; M-x eglot or for auto:
  ;; e.g. (add-hook 'foo-mode-hook 'eglot-ensure)
  )
